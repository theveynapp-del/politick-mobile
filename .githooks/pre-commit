#!/bin/sh
# Blocks commits that drop integrations the type-checker can't see.
# See scripts/check-wiring.mjs for what's guarded and why.
node scripts/check-wiring.mjs || {
  echo ""
  echo "Commit blocked. To bypass in a genuine emergency: git commit --no-verify"
  exit 1
}
